// import {redirect as redirectTo} from '@tanstack/react-router'

// import isAuthenticated from '@/auth/isAuthenticated'

/**
 * Only allow authenticated users to access the route.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars -- temp
export default function privateRoute(redirect: string) {
  // if (!isAuthenticated()) {
  //   throw redirectTo({
  //     to: Route.fullPath,
  //     search: {
  //       redirect: redirect,
  //     },
  //   })
  // }
}
